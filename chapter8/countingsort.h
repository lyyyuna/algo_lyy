#ifndef COUNTINGSORT_H_INCLUDED
#define COUNTINGSORT_H_INCLUDED

#include <stdio.h>

extern int findmax(int *array, int size);
extern void countingsort(int *array, int *sorted, int size, int max);

#endif
